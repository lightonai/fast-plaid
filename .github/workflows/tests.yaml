name: Tests

on:
  push:
    branches:
      - main
      - "**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.12", "3.13", "3.14"]
        include:
          - os: ubuntu-latest
            torch-version: "2.11.0"
          - os: macos-latest
            torch-version: "2.11.0"
          - os: windows-latest
            torch-version: "2.10.0"

    runs-on: ${{ matrix.os }}

    env:
      # PyO3 ABI3 for Python 3.14+ (no-op for ≤3.13)
      PYO3_USE_ABI3_FORWARD_COMPATIBILITY: "1"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "latest"
          python-version: ${{ matrix.python-version }}
          activate-environment: true

      - name: Install dependencies and run common tests
        run: |
          uv pip install "torch==${{ matrix.torch-version }}" maturin setuptools numpy
          uv pip install -e . --no-build-isolation
          uv pip install pytest pytest-cov
          uv run --no-sync python -c "import torch; print('torch version:', torch.__version__)"
          uv run --no-sync pytest tests/test.py

      - name: Run Ubuntu-specific tests
        if: matrix.os == 'ubuntu-latest'
        run: uv run --no-sync pytest python/fast_plaid/filtering/filtering.py
